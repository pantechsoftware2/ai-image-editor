#!/usr/bin/env node

/**
 * Quick Test: Does Image Generation Work?
 * 
 * Usage:
 *   1. Start dev server: npm run dev
 *   2. In another terminal: npm run test:image
 */

console.log(`
╔═══════════════════════════════════════════════════════════╗
║         IMAGE GENERATION TEST - SETUP INSTRUCTIONS        ║
╚═══════════════════════════════════════════════════════════╝

STEP 1: Authenticate (if not already done)
  $ gcloud auth application-default login
  → Sign in with your Google account
  → Grant permissions

STEP 2: Start the dev server (Terminal 1)
  $ npm run dev
  
  Wait for: "✅ Vertex AI SDK initialized successfully"

STEP 3: Run the test (Terminal 2)
  $ npm run test:image
  
  Should show:
  ✅ PASS: Credentials are loaded
  ✅ PASS: Generated 4 images

STEP 4: Check results
  ✅ = Image generation working
  ❌ = Something needs fixing

═══════════════════════════════════════════════════════════
`);
